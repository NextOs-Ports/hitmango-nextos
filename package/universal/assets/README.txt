Copy the complete assets/ directory from your legitimate Hitman GO 1.18.1
ARM64 Android APK into this directory's parent (hitmango/assets/).

The resulting layout must include at least:

  assets/bin/Data/boot.config
  assets/bin/Data/globalgamemanagers
  assets/bin/Data/Managed/Metadata/global-metadata.dat

Do not rename, flatten or selectively recompress the Unity data files.
